import React from "react";
import "./CounterHeader.css"

const CounterHeader = () => {
    return (
        <header>
            <h1>Counter App</h1>
            <p>Use the click button to unlock different animal badges every time you reach a threshold!, you can also reset your progress with the "Reset counter" button</p>
        </header>
    )
};

export { CounterHeader };